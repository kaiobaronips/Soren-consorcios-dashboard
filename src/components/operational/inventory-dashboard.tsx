"use client";

import { useState } from "react";
import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { inventoryClassification, inventoryTags } from "@/data/operational-mock-data";
import { FilterToolbar, MetricCard, OperationalPageHeader, Panel } from "@/components/operational/enterprise-ui";

const tooltip = { background: "#fff", border: "1px solid #c6c6c6", borderRadius: 2, fontSize: 12 };
const tabs = ["Estatísticas", "Produtos", "Alterações", "Categorias", "Clientes", "Infraestrutura", "Reconciliação"];
export function InventoryDashboard() {
  const [tab, setTab] = useState("Estatísticas");
  return <div className="space-y-6"><OperationalPageHeader eyebrow="Base comercial" title="Inventário" description="Visibilidade consolidada dos produtos, categorias e ativos operacionais."/><div className="enterprise-tabs" role="tablist">{tabs.map((item) => <button key={item} className={`enterprise-tab ${tab === item ? "enterprise-tab-active" : ""}`} role="tab" aria-selected={tab === item} onClick={() => setTab(item)}>{item}</button>)}</div><FilterToolbar/><div className="enterprise-grid"><MetricCard label="Grupos administradores" value="17" delta="2 sincronizados hoje"/><MetricCard label="Produtos cadastrados" value="151" delta="+7 no período"/><MetricCard label="Ativos publicados" value="86" delta="56,9% do inventário"/><MetricCard label="Em revisão" value="8" delta="Requerem conferência" tone="danger"/></div><div className="enterprise-grid"><Panel title="Categorias do inventário" className="col-span-12 xl:col-span-6"><div className="h-[340px] p-4"><ResponsiveContainer width="100%" height="100%"><BarChart layout="vertical" data={inventoryTags} margin={{ top: 8, right: 16, left: 60, bottom: 0 }}><CartesianGrid stroke="#e0e0e0" horizontal={false}/><XAxis type="number" axisLine={false} tickLine={false} tick={{ fontSize: 12, fill: "#6f6f6f" }}/><YAxis type="category" dataKey="name" width={94} axisLine={false} tickLine={false} tick={{ fontSize: 12, fill: "#525252" }}/><Tooltip contentStyle={tooltip}/><Bar dataKey="atual" name="Atual" fill="#4589ff"/><Bar dataKey="anterior" name="Anterior" fill="#42bea8"/></BarChart></ResponsiveContainer></div></Panel><Panel title="Classificação dos produtos" className="col-span-12 xl:col-span-6"><div className="grid h-[340px] grid-cols-1 items-center gap-4 p-4 sm:grid-cols-2"><div className="relative h-[240px]"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={inventoryClassification} dataKey="quantidade" innerRadius={66} outerRadius={95} stroke="none">{inventoryClassification.map((item) => <Cell key={item.name} fill={item.color}/>)}</Pie><Tooltip contentStyle={tooltip}/></PieChart></ResponsiveContainer><div className="pointer-events-none absolute inset-0 flex flex-col items-center justify-center"><span className="text-3xl">111</span><span className="text-xs text-[#6f6f6f]">produtos</span></div></div><div className="space-y-4">{inventoryClassification.map((item) => <div key={item.name} className="flex items-center justify-between border-b border-[#e0e0e0] pb-3 text-sm"><span className="flex items-center gap-2"><i className="size-2" style={{ background: item.color }}/>{item.name}</span><span>{item.quantidade}</span></div>)}</div></div></Panel><Panel title={`Recursos - ${tab}`} className="col-span-12"><div className="overflow-x-auto"><table className="enterprise-table"><thead><tr><th>Produto</th><th>Categoria</th><th>Administradora</th><th>Atualizado</th><th>Status</th></tr></thead><tbody>{[["Carta imobiliária 400K","Imóveis","Atlas Consórcios","há 10 min","Publicado"],["Plano veículo 120K","Veículos","Norte Capital","há 32 min","Publicado"],["Crédito empresarial","Empresarial","Soren Parceiros","há 1 h","Rascunho"]].map((row) => <tr key={row[0]}>{row.map((cell) => <td key={cell}>{cell}</td>)}</tr>)}</tbody></table></div></Panel></div></div>;
}
